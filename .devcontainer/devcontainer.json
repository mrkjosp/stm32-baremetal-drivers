{
  "name": "STM32 Bare-Metal (ARM GCC + QEMU)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y gcc-arm-none-eabi qemu-system-arm make gdb-multiarch",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.cpptools", "marus25.cortex-debug"]
    }
  }
}